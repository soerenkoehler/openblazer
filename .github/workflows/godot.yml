name: Godot

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # - name: install Godot
      #   run: ./deploy.sh install

      - name: install Microsoft Store CLI
        uses: microsoft/microsoft-store-apppublisher@v1.3

      # - name: package artifacts
      #   run: ./deploy.sh package

      - name: create release
        run: ./deploy.sh release
        env:
          GH_TOKEN: "${{ github.token }}"
          MSSTORE_.TENANT_ID:     "${{ secrets.TENANT_ID }}"
          MSSTORE_.SELLER_ID:     "${{ secrets.SELLER_ID }}"
          MSSTORE_.CLIENT_ID:     "${{ secrets.CLIENT_ID }}"
          MSSTORE_.CLIENT_SECRET: "${{ secrets.CLIENT_SECRET }}"
